{
	"$schema": "http://json-schema.org/draft-07/schema",
	"$id": "https://openslo.com/schemas/v1/parts/alertnotificationtarget-spec.schema.json",
	"title": "OpenSLO: Part -> AlertNotificationTarget Spec",
	"description": "An Alert Notification Target defines the possible targets where alert notifications should be delivered to. For example, this can be a web-hook, Slack or any other custom target.",
	"type": "object",
	"properties": {
		"spec": {
			"type": "object",
			"required": [
				"target"
			],
			"properties": {
				"target": {
					"type": "string"
				},
				"description": {
					"$ref": "https://openslo.com/schemas/v1/parts/description.schema.json"
				}
			}
		}
	}
}
